# Build artifacts
target/
**/node_modules/
frontend/dist/

# Secrets and local runtime state — must never enter an image
**/.env
state/
proofs/

# OS / editor / tooling noise (any depth)
**/.DS_Store
**/.vscode/
**/.idea/
**/.claude/
*.log

# VCS metadata
.git/

# Markdown isn't needed for the build
**/*.md
